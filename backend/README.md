Create and activate the .venv file before running anything.

Get the requirements from the requirements.txt file (pip install -r requirements.txt)

If adding anything new service like models or AI part, put them in the service dir.

Discuss before deleting or creating any new dir or files